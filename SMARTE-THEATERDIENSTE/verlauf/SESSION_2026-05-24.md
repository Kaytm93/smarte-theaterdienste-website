---
date: 2026-05-24
session: 29
milestone: M17 Welle 2
tags: [session, m17, welle-2, unterseiten, feedback]
---

# Session 29 — M17 Welle 2: Unterseiten nach Feedback 14.5.2026

## Auftrag
Website weiter anhand `Anmerkungen Entwurf 14.5.2026.md` verbessern (Vault zuerst lesen, dann Plan, dann umsetzen, Website-Design-Ultra-Plugin als Leitplanke).

## Wichtigster Befund zu Beginn
Die als „erledigt" beschriebene **Welle 1** (CI, Navigation, Landing) lag **ungemergt** im Worktree-Branch `claude/heuristic-hawking-2024ca` (`b7ade71`), genau ein Commit vor `main` (M16 `f109bf2`). `main` zeigte noch den alten Stand. → Per `git merge --ff-only` verlustfrei auf `main` gezogen (ADR-50), dann Welle 2 darauf gebaut.

## Umgesetzt (6 Schritte)
1. **Konzeption** — `TextSection` um optionales `image`/`imageAlt`/`imageCaption` (grayscale-Figur) erweitert; 3 Schwarzweiß-Sanity-Fotos in `projekt.json` (DE/EN); Team-Block (`TeamGrid`) als Abschnitt „Hinter dem Projekt"; Zeitstrahl-Abschnitt. Seite holt `listPastEvents` (`revalidate=60`).
2. **Technische Standards** — `<VideoEmbed>` (Comic-Clip `cCCa7Yuzaf0`) + `<ComicStrip>` (Frames aus `landing.json`) direkt eingebettet; Comic-Clip aus `ResourceLinkGrid` entfernt; 2 Datenmodell-Fotos.
3. **Jetzt mitmachen** — neue `MapEmbed.tsx` mit Google-MyMaps-iframe (`mid=1WQiyQkmVpOG9CNYfLoEj3qNlGg52xo8`).
4. **FAQ** — `FaqAccordion` gruppiert nach `category` + Quick-Nav; 4 lokalisierte Labels in `pages.faq.categories`.
5. **Team** — Route `ansprechpersonen`→`team` + 308-Redirects; Telefon/E-Mail aus `ContactCard`+`team.json` raus; Portrait→Bühne-Cross-Fade (`stage`-Feld, sonst Zoom-Fallback); Footer/Sitemap/Messages angepasst.
6. **Blog+Termine → Zeitstrahl** — neue `Timeline.tsx` (Supabase-Events + Genially `67531f7a9e5b6c42deba21f3`) als `#zeitstrahl` in Konzeption; `/blog`+`/termine` 308 auf `/konzeption`.

## Neue/geänderte Dateien (Auszug)
- Neu: `src/components/sections/MapEmbed.tsx`, `src/components/sections/Timeline.tsx`, `verlauf/SESSION_2026-05-24.md`
- Route: `src/app/[locale]/{ansprechpersonen → team}/page.tsx`
- Geändert: `TextSection.tsx`, `ContactCard.tsx`, `FaqAccordion.tsx`, `konzeption/page.tsx`, `konzeption/technische-standards/page.tsx`, `jetzt-mitmachen/page.tsx`, `faq/page.tsx`, `routing.ts`, `next.config.ts`, `sitemap.ts`, `Footer.tsx`, `loader.ts`, `messages/{de,en}.json`, `content/{de,en}/{team,projekt,projekt-technische-standards}.json`

## Verifikation
- `pnpm exec tsc --noEmit` clean (Quellcode fehlerfrei).
- `pnpm build`: TypeScript ✓ + Compile ✓; Abbruch nur bei `/icon` (`@vercel/og` `ETIMEDOUT`, Sandbox ohne Netz — kein Code-Fehler, auf Vercel unkritisch).
- Dev-Smoke: alle Zielseiten HTTP 200; Redirects `/ansprechpersonen`→`/team`, `/termine`+`/blog`→`/konzeption`, `/contact-persons`→`/team` alle 308.
- DOM-Checks: Team ohne `tel:`/`mailto:`; 4 FAQ-Kategorien + Quick-Nav; MyMaps-iframe; Comic-Video + 3 Comic-Frames; Konzeption mit 3 `grayscale(1)`-Fotos + Timeline (5 Events) + Genially.
- Screenshot `/de/konzeption` (oben): Bühnenverein-Logo, 4-Item-Nav, Public Sans, Purple-Akzent.

## Offen
- 4 Bühnenfoto-URLs (Sanity) für den vollen Team-Hover (`stage` in `team.json`) — Mechanik bereit, aktuell Zoom-Fallback.
- Dead-Files `blog/page.tsx` + `termine/page.tsx` (per Redirect unerreichbar) können später gelöscht werden.
- Production-Deploy (Welle 1 + 2) steht noch aus.

## Commit
Implementation: (folgt) · Vault-SHA-Commit: (folgt)

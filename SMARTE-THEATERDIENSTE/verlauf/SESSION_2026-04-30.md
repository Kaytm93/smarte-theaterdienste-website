# SESSION 2026-04-30 — Production-Deploy + Revalidate live

> Vorgänger: [[SESSION_2026-04-26b]] (M3) plus M4-Logs im [[../CHANGELOG|CHANGELOG]] · Nachfolger: TBD (M5/M8)

## Ziel

Nächsten Vault-Schritt ausführen: Vercel-Deployment einrichten, Production-URL prüfen und Supabase-Revalidate an die Live-Domain hängen.

## Was passierte

1. Vault vollständig gelesen: `START_HIER`, `KONTEXT`, `DASHBOARD`, `PROBLEME`, `MUSTER`, `ROADMAP`, `INHALTE`, `API`, `ENTSCHEIDUNGEN`, `CHANGELOG` und vorhandene Session-Dateien.
2. Vollständigen Plan vor Änderungen erstellt und vom User mit „Go" freigegeben.
3. Preflight:
   - `pnpm exec tsc --noEmit` clean
   - `pnpm exec eslint .` clean
   - `pnpm exec next build` clean, alle Content-Seiten ● SSG/ISR, `/api/revalidate` ƒ Dynamic
4. Vercel:
   - Vercel-CLI via `pnpm dlx vercel@latest` genutzt
   - Device-Login im Browser bestätigt, CLI-User `kaytm93`
   - `vercel link --yes` legte/verlinkte Projekt `kaytm93s-projects/smarte-theaterdienste-website`
   - Production-Env-Vars gesetzt: Supabase Public URL, anon key, service-role key, `REVALIDATE_SECRET`, `NEXT_PUBLIC_SITE_URL`
   - Production deployed und auf `https://smarte-theaterdienste-website.vercel.app` aliasiert
5. Deployment-Hygiene:
   - Erster CLI-Deploy warnte wegen erkannter `.env`-Datei
   - `.vercelignore` ergänzt und `.env.example` ebenfalls aus dem Upload ausgeschlossen
   - Finaler Force-Redeploy lud 115 Projektdateien und lief ohne `.env`-Warnung
6. GitHub-Integration:
   - `vercel git connect https://github.com/Kaytm93/smarte-theaterdienste-website` versucht
   - Fehler: Vercel kann Repository nicht verbinden, vermutlich GitHub-App/Rechte im Dashboard
   - Workaround dokumentiert: CLI-Deploys funktionieren, Dashboard-Verknüpfung bleibt User-Action
7. Supabase-Revalidate:
   - `pg_net` in der Cloud-DB aktiviert
   - `public.revalidate_nextjs_cache()` als gemeinsame Trigger-Funktion angelegt
   - Trigger auf `posts`, `post_translations`, `events`, `event_translations`, `faqs`, `faq_translations`
   - Kein Repo-Migration-File, weil die Trigger-Funktion die Secret-haltige Production-URL enthält
8. Live-Verifikation:
   - `/de`, `/en`, `/de/blog`, `/de/blog/kickoff-datenraum-kultur`, `/de/faq`, `/de/termine`, `/en/blog`, `/api/revalidate` → HTTP 200
   - POST `/api/revalidate?secret=wrong` → HTTP 401
   - POST mit gültigem Secret und `{ "table": "posts" }` → HTTP 200, `paths.length = 2`
   - No-op `UPDATE` in `post_translations` → `net._http_response.status_code = 200`

## Files Touched

**Neu:**
- `.vercelignore`
- `SMARTE-THEATERDIENSTE/verlauf/SESSION_2026-04-30.md`

**Geändert:**
- `SMARTE-THEATERDIENSTE/API.md`
- `SMARTE-THEATERDIENSTE/CHANGELOG.md`
- `SMARTE-THEATERDIENSTE/DASHBOARD.md`
- `SMARTE-THEATERDIENSTE/ENTSCHEIDUNGEN.md`
- `SMARTE-THEATERDIENSTE/KONTEXT.md`
- `SMARTE-THEATERDIENSTE/PROBLEME.md`
- `SMARTE-THEATERDIENSTE/ROADMAP.md`

**Nicht angefasst/staged:**
- `AGENTS.md` hatte bereits vor Arbeitsbeginn eine lokale Änderung und bleibt als User-Änderung separat.
- `.env.local`, `.vercel/`, `supabase/.temp/`

## Outcome

Production ist live, echte Supabase-Daten rendern auf der Vercel-Domain, On-Demand-Revalidate funktioniert per Cloud-Trigger. Nächster sinnvoller Produkt-Schritt ist M5 Partner-Karte; optional vorher Vercel-GitHub-Integration im Dashboard verbinden.

# Session 2026-05-19 — M17 Welle 1: CI-Refresh + Nav + Landing

## Auftrag

User-Lieferung im Downloads-Ordner `~/Downloads/smarte-theaterdienste/`:

- `Smarte Theaterdienste/Anmerkungen Entwurf 14.5.2026.md` — Seite-für-Seite-Anmerkungen.
- `Smarte Theaterdienste/Design-Vorlage.md` + `STD-Colors+Fonts.pdf` — verbindliche Corporate Identity.

Ziel: Website auf die Bühnenverein-CI heben, Anmerkungen aus dem Feedback-Dokument abarbeiten. User-Entscheidung in der Plan-Phase: Welle 1 jetzt (Foundation + Nav + Landing), Welle 2 (Page-Redesigns, Team-Relocation, Zeitstrahl) später; URLs ziehen mit, Theater-Bühnen-Hover bleibt für Welle 2.

## Vault-Lesen

`START_HIER`, `KONTEXT`, `DASHBOARD`, `PROBLEME`, `MUSTER`, `INHALTE`, `CHANGELOG`, `ENTSCHEIDUNGEN`, `ROADMAP`, `GO_LIVE_CHECKLIST` plus die neuen User-Quellen. Außerdem das repo-lokale Plugin `plugins/website-design-ultra/skills/{core-rules,typography}/SKILL.md` als Design-Referenz; die Hard-Banned-Patterns wurden eingehalten (eine Akzentfarbe, keine Lila-Gradients, kein zentrierter Hero, `min-h-[100dvh]`-Erbe).

## Phase A — Corporate-Identity-Foundation

- `src/app/[locale]/layout.tsx`: `Public_Sans` (variable, italic) als einzige Schriftart importiert; `Newsreader`/`Geist`-Imports raus. `Geist_Mono` bleibt als Mono-Akzent für Editorial-ToC-Nummern.
- `src/styles/tokens.css`: komplettes Rewrite auf CI-Hex-Palette aus `STD-Colors+Fonts.pdf`:
  - Surfaces: Lucent White `#FAFAFF`, Gray10 `#E5E5E5`, Gray20 `#BEBEBE`, weißes Highlight `#FFFFFF`.
  - Ink: Black `#000000`, Gray80 `#333333`, Gray60 `#666666`, Gray40 `#999999`.
  - Akzent: Purple Dark `#BF93E1`, Purple Medium `#E1B9FF`, Purple Light `#EFDAFF`, dazu der AA-konforme Helper `--accent-brand-ink #6B4A9A` für Text auf hellem Grund.
  - Schatten via `color-mix(in srgb, var(--ink) x%, transparent)`.
- `src/app/globals.css`: `@theme inline`-Font-Bridges (`--font-sans`/`--font-heading`/`--font-serif`) zeigen auf `--font-public-sans`. `editorial-kicker::before` und Drop-Cap auf Purple Dark; Editorial-Square zu Purple-Punkt. Dark-Mode-Surface auf Schwarz/Gray-Schema kalibriert.
- Bulk-Replacements in tsx/ts/css:
  - `var(--accent-secondary)` → `var(--accent-brand)` (Rubrik-Rot komplett raus).
  - `text-[var(--accent-brand)]` → `text-[var(--accent-brand-ink)]` (Kontrastfix für Body-Text auf Lucent White).

## Phase B — Navigation & Routing

- `src/lib/i18n/routing.ts`: `pathnames`-Map auf neue Internal-Keys umgestellt:
  - `/konzeption` (DE) ↔ `/concept` (EN); Sub-Routes `technische-standards`, `semantische-standards`.
  - `/jetzt-mitmachen` (DE) ↔ `/join` (EN); Sub-Routes `anwendungsbeispiele`, `mitwirkung`.
  - NEU `/materialien` (DE) ↔ `/materials` (EN).
- Dateisystem-Renames per `git mv`:
  - `src/app/[locale]/projekt/` → `src/app/[locale]/konzeption/`.
  - `src/app/[locale]/beteiligung/` → `src/app/[locale]/jetzt-mitmachen/`.
- `next.config.ts`: 12 permanente 308-Redirects (`/de/projekt`→`/de/konzeption`, plus alle Sub-Routes, plus EN-Pendants).
- Hrefs in tsx/JSON-Content per `find`+`sed`-Loop migriert.
- `src/components/layout/Header.tsx` neu geschrieben:
  - Editorial-Top-Rail (`Ausgabe 03 · Datenraum Kultur · JSON / ORIF / DRK`) entfernt.
  - Bühnenverein-Logo links neben dem Sitenamen prominent platziert (Trust-Signal).
  - `NAV_ITEMS` reduziert auf vier: Konzeption, Jetzt mitmachen, Materialien, FAQ.
- `src/components/layout/MobileNav.tsx`: gleiche 4-Item-Liste.
- `src/components/layout/Footer.tsx` `FOOTER_NAV` enthält weiterhin alle Routes inkl. Blog/Termine/Ansprechpersonen, damit Inhalte erreichbar bleiben.
- `src/messages/de.json` + `en.json`: `nav.materials` ergänzt, `nav.edition`/`nav.dateline` raus, Hero-Strings korrigiert (siehe Phase C), `pages.materialien.*` neu, `pages.ansprechpersonen.title` auf „Team", `pages.beteiligung.title` auf „Jetzt mitmachen".
- `src/content/{de,en}/materialien.json` neu: 8 ORIF-Werkzeuge (Comic-Clip, Image-Video, Infomaterial, Musterkalkulation, ORIF-Doku, Validator, Lektoratstool, GitHub-Repo) plus zwei Folge-Links.
- `src/lib/content/loader.ts`: `materialien`-Eintrag in `REGISTRY` ergänzt.
- `src/app/[locale]/materialien/page.tsx` neu: PageHero + ResourceLinkGrid + CTA-Links.
- `src/app/sitemap.ts`: `STATIC_HREFS` aktualisiert (alte raus, neue rein).

## Phase C — Landing-Redesign

- `src/app/[locale]/page.tsx`:
  - Editorial-Top-Rail mit „AUSGABE 03 · JSON · ORIF · Datenraum Kultur" entfernt.
  - Neuer `<BuehnenvereinLockup>` direkt oberhalb der Headline.
  - `hero.kicker` deemphasized auf kleine Caption-Zeile unter dem Subtitle.
  - `hero.tags` umgeschrieben auf `Schema.org + GND + JSON = ORIF` (DE + EN).
  - `figcaption` „USE CASE 03" entfernt; Polaroid trägt jetzt nur eine ruhige Schriftzeile `USE CASE 3 · DATENRAUM KULTUR`.
  - Theater-Parade-Foto erhält `grayscale`-Treatment (CI: Fotos schwarz-weiß).
- Neue Komponenten unter `src/components/sections/`:
  - `BuehnenvereinLockup.tsx` — Logo + Klein-Label „Ein Projekt des" mit Purple-Pale-Halo-Punkt.
  - `VideoEmbed.tsx` — Server-Component, lazy `youtube-nocookie.com`-Iframe in 16:9.
  - `QuoteGallery.tsx` — 3-spaltige Karten mit Purple-Quote-Mark, italic Body, Footer.
- `src/content/{de,en}/landing.json`: neue Blöcke `imageVideo` (YouTube-ID `B4vQwW5ICk8`), `trust` (Lockup-Label), `quotes` (3 Default-Stimmen).
- `next.config.ts.images.remotePatterns`: `i.ytimg.com` für optionale Video-Poster ergänzt.

## Verifikation

- `pnpm typecheck` ✅
- `pnpm lint` ✅
- `pnpm build` ✅ — 37 Pages SSG inkl. neuer `/de/materialien` und `/en/materials`; alte Slugs sind in der Build-Liste nicht mehr enthalten.
- Preview-MCP auf Port 3000:
  - `/de` Hero zeigt exakt die User-Wünsche: Bühnenverein-Lockup oben, Headline „Smarte Theaterdienste" in Public Sans bold, Subtitle mit Purple-Linke, Tag-Zeile `EIN PROJEKT DES DEUTSCHEN BÜHNENVEREINS · Schema.org + GND + JSON = ORIF`, CTAs „Jetzt mitmachen" + „So funktioniert's", Theater-Parade-Polaroid mit ruhiger Caption.
  - Header-Smoke: Bühnenverein-Logo + Smarte Theaterdienste + Konzeption / Jetzt mitmachen / Materialien / FAQ + EN-Switch.
  - `/de/materialien` lädt PageHero + ResourceLinkGrid (8 Karten) + CTA-Links.
- HTTP-Smoke:
  - 200: `/de`, `/de/konzeption`, `/de/jetzt-mitmachen`, `/de/materialien`, `/de/faq`, `/en`, `/en/concept`, `/en/join`, `/en/materials`.
  - 308 → korrekte neue URL: `/de/projekt`, `/de/projekt/technische-standards`, `/de/beteiligung`, `/de/beteiligung/mitwirkung`, `/en/project`, `/en/participation`.

## Status am Ende

Welle 1 ist lokal production-validiert; Code committet (folgt direkt unten), Push und Obsidian-Sync ebenfalls in dieser Session erledigt. Welle 2 (Konzeption mehr Bilder · Tech-Standards Comic+Video direkt embedden · Mitwirkung Google-Maps-iframe · FAQ-Kategorien · Team-Relocation mit CSS-Hover · Zeitstrahl-Route mit Genially) bleibt offen.

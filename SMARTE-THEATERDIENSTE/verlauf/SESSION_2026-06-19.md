# Session 37 — Native interaktive „Spielplan-Reise" + Partnerkarte neu

**Datum:** 2026-06-19
**Commit-SHA (Code):** `ccf6a77`
**Themen:** Genially-Embed → native GSAP-Komponente · Partnerkarte-Redesign · `node_modules`-Reparatur

---

## Anlass

User (nach Vault-Lesen) mit zwei Aufträgen:

1. **„Interaktive Reise durch die Spielplanverbreitung"** (`/konzeption`): Das eingebettete Genially sieht schlecht aus — „du musst aber das alles selber bauen, animiert natürlich" (Website-Design-Ultra-Referenz).
2. **„Wer schon dabei ist"** (`/jetzt-mitmachen/mitwirkung`): Abschnitt aktualisieren — „die Karte passt nicht, die ist nicht vollständig".

## Vorgehen

### 0 — `node_modules`-Korruption (Blocker vorab)
Dev-Server crashte beim Boot: `SyntaxError: Unexpected end of JSON input` in `next/dist/compiled/semver/package.json`, danach `_picocolors.bold is not a function`, dann leeres `@parcel/watcher/.../node-addon-api/package.json`. `pnpm store status` meldete fehlende Index-Dateien (ENOENT). `pnpm install` half nicht (Lockfile „up to date"). **Fix:** `rm -rf node_modules && pnpm install` (8,1 s). Danach `✓ Ready`. → [[../PROBLEME]].

### 1 — Diagnose im Browser
Preview-Server (Port 3000). `/de/konzeption`: Genially-iframe (`view.genially.com/67531f7a9e5b6c42deba21f3`) zeigte nur den schwarzen Genially-Spinner — Screenshot bestätigt das „sieht schlecht aus". `/de/jetzt-mitmachen/mitwirkung`: ausgewaschene Wikimedia-Hochformat-Rasterkarte (blaues Meer), 4 blasse, kaum sichtbare Pulse-Punkte, faded Text — das „passt nicht / nicht vollständig". Supabase ist lokal konfiguriert (4 Partner rendern).

### 2 — Task 1: native `SpielplanReise`
Neue Client-Komponente `src/components/sections/SpielplanReise.tsx` (`useGSAP`): 5-Stationen-Stepper (Status quo → Gemeinsame Sprache/Standards → ORIF-Schnittstelle → Konnektor/Datenraum → Verbreitung). Icon-Knoten (Lucide), animierte Fortschrittslinie (von Knoten 1 bis 5), Bühnen-Card (Index/Tag/Phase/Titel/Body + Format-Chips, Station 1 dashed-muted), Prev/Next + klickbare Knoten, ARIA-`tablist/tab/tabpanel` + Pfeil/Home/End, `prefers-reduced-motion`-Guard, Dauer-Puls am aktiven Knoten. `Timeline.tsx` von Genially entkoppelt; `konzeption/page.tsx` reicht `timeline.journey` durch. → [[../ENTSCHEIDUNGEN#ADR-62]].

### 3 — Task 2: Partnerkarte neu
`PartnerMapClient.tsx` neu: CI-Graustufenkarte (`filter: grayscale(1) …` + `opacity-70`, `max-w-[420px]`), kräftige **nummerierte** Marker (1–4, Marker ↔ Liste teilen Index), GSAP-`back.out`-Eindrop, `animate-ping`-Ring auf Auswahl, nummerierte klickbare Liste mit Koordinaten/Website-Link, Netzwerk-Hinweis „Über 140 Institutionen" + CTA `/`. Daten weiter Supabase. → [[../ENTSCHEIDUNGEN#ADR-63]].

## Geänderte Bereiche

- `src/components/sections/SpielplanReise.tsx` (NEU)
- `src/components/sections/Timeline.tsx` (Genially → SpielplanReise)
- `src/components/sections/PartnerMapClient.tsx` (Redesign)
- `src/app/[locale]/konzeption/page.tsx` (journey durchreichen, GENIALLY_URL raus)
- `src/messages/de.json`, `src/messages/en.json` (`timeline.journey`-Block; `pages.mitwirkung.map` Lead + `networkNote`/`networkCta`)

## Verifikation

- **Preview DE+EN:** Reise (`/de/konzeption`, `/en/concept`): 5 Tabs, Stationswechsel, Progress „Station x von 5"/„x of 5", Fortschrittsfüllung 0→100 %, EN-Texte korrekt. Karte (`/de/jetzt-mitmachen/mitwirkung`, `/en/join/contribute`): 4 nummerierte Marker, Selektion mit Ping + Koordinaten, Netzwerk-Hinweis DE/EN.
- **Mobile 375 px:** beide `docOverflowPx 0`. Karte hatte 20 px Overflow (aspect-in-grid) → `grid-cols-1` + `min-w-0` behoben. Stepper passt (Knoten 29→346 px).
- **Console:** sauber (frühe `FORMATTING_ERROR` waren stale vom ersten Render vor dem `t.raw`-Fix).
- **Build:** `tsc --noEmit` clean, ESLint clean, `pnpm build` „✓ Compiled successfully", **35/35 SSG**-Seiten, 0 Fehler/Warnings.

## Hinweise / Stolperfallen

- **ICU + Server-Komponente:** `progressLabel` („Station {current} von {total}") muss als **Roh-String** (`t.raw`) durchgereicht werden — `t("…")` formatiert serverseitig und wirft `FORMATTING_ERROR`, weil die Platzhalter fehlen. Ersetzung client-seitig.
- **`origin/main [gone]`** trat erneut auf (verwaister Ref-Lock, gleiche Klasse wie 2026-06-08/-10) — im Git-Schritt non-destruktiv behoben (read-only `git ls-remote`/`fetch` zuerst).

## Ergebnis

Beide User-Aufträge umgesetzt und verifiziert. Genially-Embed entfernt (kein Third-Party-iframe mehr auf `/konzeption`), Partnerkarte on-brand + „vollständig" wirkend. Build production-ready.

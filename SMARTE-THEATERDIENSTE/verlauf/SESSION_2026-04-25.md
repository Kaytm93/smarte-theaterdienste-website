# Session 2026-04-25 — M1 Setup & Infra + Vault-Initialisierung

**Dauer:** ~1 Session
**Ziel:** Projekt von Null aufsetzen bis lauffähiges DE/EN-Skeleton + GitHub-Push + Obsidian-Vault.

---

## Zeitlinie

1. **Plan-Phase**
   - User-Brief mit kompletter Miro-Inhaltsstruktur, Tech-Stack, Auto-Pilot-Anweisung
   - Plan in `/Users/kaygewinner/.claude/plans/projekt-smarte-theaterdienste-breezy-moth.md` geschrieben
   - 3 Klärungsfragen via AskUserQuestion: Pfad, GitHub, Karte → alle 3 mit Recommended beantwortet

2. **Tooling-Check**
   - Node 20.19.4 ✓, git ✓, npm 10.8.2 ✓
   - pnpm/gh/supabase fehlten; Homebrew fehlte
   - User bestätigte „alle Tools installieren" + verwies auf vorhandenen GitHub-SSH-Key
   - pnpm via `npm install -g pnpm` (nvm-Prefix, kein sudo)
   - Supabase CLI nicht global installierbar (Postinstall-Verbot) → später als dev-dep
   - gh CLI deferred → User legt Repo via Web an

3. **Bootstrap**
   - `pnpm create next-app@latest smarte-theaterdienste-website --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-pnpm --turbopack --skip-install --yes`
   - Lieferte **Next.js 16.2.4** (statt geplant 15)
   - `pnpm install` für Base-Deps
   - `pnpm add next-intl gsap @supabase/ssr @supabase/supabase-js clsx tailwind-merge class-variance-authority lucide-react`

4. **Next.js-16-Doku konsultiert**
   - `node_modules/next/dist/docs/01-app/02-guides/upgrading/version-16.md` gelesen
   - Breaking Changes notiert: proxy.ts statt middleware.ts, async params, revalidateTag-Profile, Turbopack-Default

5. **i18n-Setup**
   - `next.config.ts` mit `withNextIntl("./src/lib/i18n/request.ts")` + `images.remotePatterns`
   - `src/lib/i18n/routing.ts` mit `defineRouting`, locales `['de','en']`, defaultLocale `de`, localePrefix `always`, vollständige `pathnames`-Map (DE↔EN für alle Sektionen)
   - `src/lib/i18n/request.ts` mit `getRequestConfig` und `hasLocale`
   - `src/lib/i18n/navigation.ts` mit `createNavigation(routing)`
   - `src/proxy.ts` mit `createMiddleware(routing)` und `matcher: "/((?!api|_next|_vercel|.*\\..*).*)"`

6. **Layout-Restrukturierung**
   - Alte `src/app/layout.tsx` und `page.tsx` gelöscht
   - `src/app/[locale]/layout.tsx`: html/body, NextIntlClientProvider, Geist-Fonts, generateMetadata mit `getTranslations`, `setRequestLocale`, `generateStaticParams`
   - `src/app/[locale]/page.tsx`: Skeleton-Hero mit Kicker, Title, Subtitle, 2× CTA, Inline-Nav

7. **Helpers + Stubs**
   - `src/lib/utils.ts` (cn)
   - `src/styles/tokens.css` (Easings, Durations)
   - `src/lib/gsap/registerScrollTrigger.ts`
   - `src/lib/supabase/{client,server}.ts` (Stubs für M4)

8. **Messages**
   - `src/messages/de.json`, `en.json` mit `meta`, `nav`, `hero`, `footer` Namespaces

9. **Meta-Files**
   - `README.md` (Quickstart, Tech-Stack, Next.js-16-Hinweise)
   - `.env.example` (Supabase, Resend, Revalidate-Secret)
   - `.gitignore` erweitert (Vault-Workspace-Cache aus, Supabase-Temp aus)

10. **Verifikation**
    - `pnpm exec tsc --noEmit` → clean
    - Dev-Server via Preview MCP auf Port 3030 gestartet (.claude/launch.json im Workspace-Root erweitert)
    - `/de` rendert: „Smarte Theaterdienste" mit allen i18n-Strings
    - `/en` rendert: „Smart Theatre Services"
    - `/` → Locale-Detection-Redirect funktioniert
    - Mobile (375x812) + Desktop (1280x800) screenshots ok
    - `pnpm build` produziert beide Locales als SSG, Proxy als Middleware registriert, keine Fehler

11. **Git + GitHub**
    - `git add` selektiv (specific files, nicht `-A`)
    - Commit `a994cd5`: „M1: Setup & Infra – Next.js 16, next-intl, Supabase-Skeleton"
    - User legt Repo `Kaytm93/smarte-theaterdienste-website` (public) im Web an
    - User gibt SSH-URL: `git@github.com:Kaytm93/smarte-theaterdienste-website.git`
    - `git remote add origin` + `git push -u origin main` → erfolgreich

12. **Vault-Initialisierung (auf User-Wunsch)**
    - Erstes _vault/ war Obsidian-Substruktur → User wünscht Nexus-Pattern (flach)
    - `SMARTE-THEATERDIENSTE/` neu angelegt mit START_HIER, KONTEXT, DASHBOARD, PROBLEME, ROADMAP, MUSTER, ENTSCHEIDUNGEN, CHANGELOG, INHALTE, API + verlauf/
    - `_vault/` gelöscht
    - `CLAUDE.md` aktualisiert
    - Commit + Push

13. **Server gestoppt**

---

## Wichtigste Entscheidungen

Siehe [[../ENTSCHEIDUNGEN|ENTSCHEIDUNGEN]] für ADRs 01–15.

## Was war überraschend

- **Next.js 16** statt 15 (frische Version) — Breaking Changes brauchten 5 min Doku-Lesen, sonst hätte ich `middleware.ts` geschrieben
- **Tailwind v4** ohne `tailwind.config.ts` (alles in CSS via `@theme inline`)
- **Locale-Detection sendet auf /en**, weil Test-Browser Englisch als Sprache hatte — User-Erwartung sollte geklärt werden (siehe PROBLEME.md)

## Was ist NICHT passiert (bewusst aufgeschoben)

- Vercel-Setup (M8)
- Supabase-Projekt anlegen (M4)
- shadcn init (M2)
- Animation-Primitives (M2)
- Inhaltliche Texte (M3)

## Übergabe an nächste Session

→ Lies `START_HIER.md`, dann `KONTEXT.md`, dann `DASHBOARD.md`. Default-nächster-Schritt: **M2**.

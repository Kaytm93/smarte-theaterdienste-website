# SESSION 2026-04-26 (b) — M3 Statische Seiten DE

> Vorgänger: [[SESSION_2026-04-26]] (M2) · Nachfolger: TBD (M4)

## Ziel

M3 abschließen: alle statischen Routen aus `routing.ts` mit Inhalten aus `INHALTE.md` füllen, Akzentfarbe definieren, Coming-Soon-Stubs für die noch nicht implementierten dynamischen Routen (Blog/FAQ/Termine) anlegen, EN-Stub-Übersetzungen mitziehen.

## User-Input

4 Entscheidungen via AskUserQuestion vorab:
1. **Akzentfarbe:** Datenraum-Blau (kühl) → `oklch(0.55 0.16 250)`
2. **Blog/FAQ/Termine:** Coming-Soon-Stubs (statt Nav-Entfernung oder 404)
3. **Impressum/Datenschutz:** sichtbare TODO-Platzhalter ("Wird vom Bühnenverein geliefert")
4. **EN-Übersetzungen:** EN-Stubs zulässig, vollständige Übersetzung erst in M7

## Was passierte (chronologisch)

1. **Vault gelesen** — START_HIER, DASHBOARD, KONTEXT, MUSTER, PROBLEME, INHALTE, ROADMAP, CHANGELOG → klares Bild von M3-Scope
2. **Aktuellen Code-Stand erfasst** — `routing.ts`, `messages/{de,en}.json`, `app/[locale]/{layout,page}.tsx`, `components/{layout,animations,ui}/`, `styles/tokens.css`, `globals.css`
3. **Plan erstellt** unter `/Users/kaygewinner/.claude/plans/lese-dir-die-diese-toasty-reddy.md` — User-Approval erhalten
4. **Implementierung in Reihenfolge:**
   - Akzentfarbe in `tokens.css` + Bridge in `globals.css`
   - `messages/{de,en}.json` erweitert (comingSoon, team-Labels, pages.*-Titel)
   - 9 Content-JSON-Bundles pro Locale (10 mit landing.json)
   - `lib/content/loader.ts` als typisierte Bundle-Registry
   - 8 neue Sections-Components
   - 10 neue Page-Routen (Server-Components mit `await params`, `setRequestLocale`, `generateMetadata`)
   - Landing erweitert um `<ComicStrip>` + `<TextSection>` für Pitch
5. **Verifikation:**
   - tsc clean (Bug zwischendurch: ASCII-Quote `"` als Schluss von `„…` zerschoss JSON; Fix mit U+201C `“`)
   - eslint clean (typografisch korrektes „…“ in ContactCard fix)
   - `pnpm build` clean: 29 SSG-Pages
   - Browser-Test via Preview-MCP: `/de`, `/de/ansprechpersonen`, `/en/contact-persons`, `/de/blog` (Coming-Soon), `/en/project/technical-standards` (tiefe EN-Slug-Map)
6. **Vault-Update:**
   - `DASHBOARD.md` → M3 ✅, M4 als next mit Voraussetzungen
   - `KONTEXT.md` → erweiterte Dateipfade-Tree mit allen neuen Routen + Sections + Content
   - `PROBLEME.md` → 3 neue „Gelöst"-Einträge (Akzentfarbe, Coming-Soon-Stubs, JSON-Quote-Bug); offene Asset-Lieferungen markiert
   - `ROADMAP.md` → M2 ✅, M3 ✅
   - `ENTSCHEIDUNGEN.md` → ADR-22 (Akzentfarbe-Wert), ADR-23 (Content-JSON-Pattern), ADR-24 (Coming-Soon-Stubs), ADR-25 (TODO-Legal-Marker)
   - `CHANGELOG.md` → Session-3-Eintrag

## Files Touched (zusammengefasst)

**Neu (32 Files):**
- `src/styles/tokens.css` (überschrieben mit Akzent-Wert) – nicht neu, aber wesentlich
- `src/content/de/{team,projekt,projekt-technische-standards,projekt-semantische-standards,beteiligung,beteiligung-anwendungsbeispiele,beteiligung-mitwirkung,legal,landing}.json` (9)
- `src/content/en/` dieselben 9 Dateien
- `src/lib/content/loader.ts`
- `src/components/sections/{PageHero,TextSection,ContactCard,TeamGrid,UseCaseCard,StepCard,ComingSoonHero,ComicStrip}.tsx` (8)
- `src/app/[locale]/{ansprechpersonen,projekt/technische-standards,projekt/semantische-standards,beteiligung/anwendungsbeispiele,beteiligung/mitwirkung,impressum,datenschutz,blog,faq,termine}/page.tsx` und `src/app/[locale]/projekt/page.tsx`, `src/app/[locale]/beteiligung/page.tsx` (12)

**Geändert:**
- `src/styles/tokens.css` (Akzent-Wert)
- `src/app/globals.css` (--color-accent-brand-foreground bridge)
- `src/messages/{de,en}.json` (comingSoon, team, pages.*)
- `src/app/[locale]/page.tsx` (Landing erweitert)

**Vault-Updates:**
- `SMARTE-THEATERDIENSTE/{DASHBOARD,KONTEXT,PROBLEME,ROADMAP,ENTSCHEIDUNGEN,CHANGELOG}.md`
- `SMARTE-THEATERDIENSTE/verlauf/SESSION_2026-04-26b.md` (diese Datei)

## Was nicht passierte (bewusst)

- Echte Portraits / Partner-Logos (User liefert)
- Animation-Polish auf Comic-Strip (M6: pinned horizontal scroll oder vertical stagger)
- Partner-Deutschlandkarte (M5; in `/beteiligung/mitwirkung` nur als Platzhalter-Box mit „Karte folgt in M5")
- Supabase-Pages für Blog/FAQ/Termine (M4)
- Vollständige EN-Übersetzungen — EN-Stubs sind komplett, aber nicht alle Texte sind richtig idiomatisch
- SEO-Erweiterungen (OG-Images, Sitemap, hreflang) — M8

## Stand am Ende

- 14 Routen × 2 Locales = 28 SSG-Pages, alle rendern, alle linkbar
- Akzentfarbe Datenraum-Blau live (Kicker, UseCase-Icons, StepCards, Quote-Border)
- Coming-Soon-Stubs schließen Nav-Lücken bis M4
- TODO-Marker auf Impressum/Datenschutz sind sofort sichtbar (gewollt)
- Build clean, types clean, lint clean

## Nächster Schritt

**M4 — Dynamic Content** (Supabase). Vor Start braucht es vom User:
- Supabase-Cloud-Projekt (EU-Region, Frankfurt empfohlen)
- ENV-Vars für `.env.local`: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`
- `REVALIDATE_SECRET` (32+ chars) für Webhook-Auth

# Session 34 — 2026-06-10: Design-Audit + Production-Polish + Deploy

**Code-Commit:** `6a8db83` · **Auftrag:** Vault lesen, „die ganze Website auf Design und Perfektion untersuchen" (Website-Design-Ultra-Plugin als Referenz), Bedingungen aus `Anmerkungen Entwurf 14.5.2026.md` einhalten, Fokus Platzierung/Abstände, Production-Polish + anschließender Deploy.

## Vorab: Git-Reparatur

`git status -sb` zeigte `origin/main [gone]`. Ursache: verwaister `.git/refs/remotes/origin/main.lock` vom Session-33-Abbruch (Jun 8) + Finder-Duplikat `index 2.lock` im Worktree. Read-only verifiziert (`git ls-remote`: Remote-`main` = lokale HEAD `8c2b661`, keine Divergenz), kein git-Prozess aktiv, Locks entfernt, `git fetch` stellte die Tracking-Ref non-destruktiv her. Dritte Instanz der Lock-Klasse (2026-05-10, 2026-06-08).

## Audit-Vorgehen

- **Referenzen:** `plugins/website-design-ultra` (core-rules + /audit-Workflow), `Anmerkungen Entwurf 14.5.2026.md` (in `~/Downloads/smarte-theaterdienste/`), Vault (KONTEXT/DASHBOARD/PROBLEME/MUSTER).
- **Abdeckung:** 13 Routen DE (+ EN-Stichproben) auf 375 px (Mobile-Emulation), 673 px (native Pane) und 1280 px (Desktop-Emulation).
- **Anmerkungen-Abgleich:** alle 14 Punkte des Entwurfs-Feedbacks sind umgesetzt (CI/Public Sans, AUSGABE-03 raus, ORIF-Formel, 4-Item-Menü, Use-Case-Übertitel reduziert, Bühnenverein-Lockup, Zitate-Galerie, Konzeption bebildert + Team + Zeitstrahl, Comic/Videos eingebettet, Image-Video auf Start, interaktive Karte auf Start + Jetzt-mitmachen, FAQ-Kategorien, Team ohne Kontaktdaten mit Hover, Blog/Termine im Zeitstrahl).
- **Messungen:** `docOverflowPx 0` überall; Grid-Spalten per JS exakt (FeatureGrid 3×400, Stakeholder 4×300, Quotes 3×387, Netzwerk-Segmente 2×2 à 225); Bild-Ladezustände ok (lazy unter Fold), 4 Marker der PartnerMap sichtbar, MyMaps-iframes auf beiden Seiten funktional.

## Befunde + Fixes (alle in `6a8db83`)

| # | Schwere | Befund | Fix |
|---|---|---|---|
| 1 | 🔴 | `NetworkMapSection`-figcaption hartkodiert deutsch — `/en` zeigte „DACH-Netzwerk · Datenraum Kultur" | `mapCaption`-Prop aus `landing.json` (EN „DACH network · Cultural Data Space") |
| 2 | 🟡 | Hero: „Ein Projekt des Deutschen Bühnenvereins" 3× im ersten Viewport (Header-Logo, Lockup, Text-Kicker); Mobile: Trenner-„·" verwaist am Zeilenende | Text-Kicker + Punkt entfernt, Mono-Formel `Schema.org + GND + JSON = ORIF` bleibt; `hero.kicker`-Key gelöscht. ADR-61 |
| 3 | 🟡 | Logo-Console-Warning 8×/Session: deklarierte Ratios (88×32, 180×48) ≠ intrinsisch 1043×368 → Next-XOR-Check feuerte bei `sm:h-8` | Intrinsische Maße deklariert, CSS bestimmt Anzeigegröße, `sizes`-Hint; `priority`→`preload` (Next-16-Deprecation) |
| 4 | 🟡 | „Senden und empfangen über den DRK" + „(DRK)" in technische-standards (DE) — Anmerkungen verbieten die Abkürzung | „… über den Datenraum Kultur", Klammer raus; Site DRK-frei (EN war sauber) |
| 5 | 🟢 | Footer: nacktes „© 2026" | `© {year} Deutscher Bühnenverein` via `footer.copyright` (DE/EN) |

**Nicht fixbar (dokumentiert):** FAQ-Frage mit Bindestrich statt Halbgeviertstrich liegt in der Cloud-DB (Migration `20260507153000`); DB-Passwort fehlt → in PROBLEME als Restposten notiert.

## Verifikation

- `pnpm typecheck`, `pnpm lint`, `pnpm build` clean (35/35 SSG).
- Preview-MCP nach `rm -rf .next` (stale-Turbopack-Klassiker: Message-JSON-Edit kam im laufenden Dev-Server nicht an → MISSING_MESSAGE): `/de` + `/en` Hero ohne Dopplung und ohne Waisen-Punkt, Karten-Caption lokalisiert, Footer-Copyright gerendert, „DRK" nirgends mehr im Seitentext, Console 0 Warnings/Errors.
- Production-Deploy via Auto-Deploy (ADR-58) nach Push; Alias-Smoke siehe DASHBOARD/CHANGELOG.

## Offen danach

- Team-Bühnenfotos (P1), Comic-Strip-Erweiterung + Legal-Texte (P2, Auftraggeber), Event-Foto-Assets + DB-Push (P2), Custom-Domain-DNS-Switch, Startseiten-Perf-Re-Audit (2 lazy iframes), FAQ-Typografie bei nächstem DB-Zugriff.

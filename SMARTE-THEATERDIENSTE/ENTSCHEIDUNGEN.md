# 🧠 Architekturentscheidungen (ADRs)

| ADR    | Datum       | Entscheidung | Begründung |
|--------|-------------|--------------|------------|
| ADR-01 | 2026-04-25  | **Next.js 16** statt geplant 15 | `create-next-app` lieferte 16.2.4. Breaking Changes (proxy.ts, async params, revalidateTag-Profile) berücksichtigt. |
| ADR-02 | 2026-04-25  | **pnpm** als Package Manager | Schnell, disk-effizient, keine sudo nötig dank nvm-Prefix. Installation via `npm install -g pnpm`. |
| ADR-03 | 2026-04-25  | **Supabase CLI als dev-dep**, nicht global | npm-global wird vom Postinstall-Script verboten. Wird in M4 als `pnpm add -D supabase` ergänzt. |
| ADR-04 | 2026-04-25  | **GitHub-Repo manuell** im Web anlegen | `gh` CLI nicht installiert, Homebrew-Install interaktiv. SSH-Key existiert bereits → `git push` reicht. |
| ADR-05 | 2026-04-25  | **Locale-Prefix für beide Sprachen** (`/de`, `/en`) | SEO + hreflang sauber, kein „naked default", konsistent. |
| ADR-06 | 2026-04-25  | **Statisches Team in `content/{locale}/team.json`** statt DB | Nur 4 Personen, kein CMS-Bedarf, keine RLS nötig. |
| ADR-07 | 2026-04-25  | **`proxy.ts` statt `middleware.ts`** | Next.js 16 Pflicht-Rename. Funktion heißt `proxy`, nur nodejs-runtime. |
| ADR-08 | 2026-04-25  | **Tailwind v4 ohne tailwind.config.ts** | Tokens via `@theme inline` in CSS. Modernster Ansatz, weniger Boilerplate. |
| ADR-09 | 2026-04-25  | **shadcn/ui als Komponentenbasis** (statt Material/Mantine/Antd) | Copy-Paste-Architektur, volle Kontrolle, kein Lock-in, ideal für Custom-Design. Radix unter der Haube. |
| ADR-10 | 2026-04-25  | **next-intl statt eigenem i18n** | RSC-native, type-safe Messages, pathnames-Map, Mehrwochen-Sparen. |
| ADR-11 | 2026-04-25  | **SVG-Karte für Partner statt Leaflet** | ~20–50 Partner, keine Geo-Search nötig, GSAP-animierbar, top Performance. |
| ADR-12 | 2026-04-25  | **ISR + on-demand Revalidate** statt SSR für dynamische Inhalte | CDN-Cache, schnelle TTFB, bei Pflege via Supabase-Webhook sofort frisch. |
| ADR-13 | 2026-04-25  | **Server Actions statt API-Routes** für Forms | Kein eigenes API-Layer, server-only secrets, type-safe. |
| ADR-14 | 2026-04-25  | **Obsidian-Vault als `SMARTE-THEATERDIENSTE/`** im Projekt-Root, Nexus-Pattern | Plan und Code synchron, single source of truth, frische Sessions können sich orientieren. |
| ADR-15 | 2026-04-25  | **Port 3030** für Dev-Server | Konflikt vermeidet mit aether (5173) und nexus (5174). Konfiguriert in `.claude/launch.json` Workspace-Root. |
| ADR-16 | 2026-04-26  | **Geist Sans für Headlines & Body** | Bereits installiert, clean-modern, kein Lizenz-Overhead. User-Entscheidung gegen Serif-Optionen (Cormorant/IBM Plex). Headlines wirken sachlich-zeitgenössisch, passt zur DRK-Tech-Brücke. |
| ADR-17 | 2026-04-26  | **shadcn-Preset radix-nova mit neutral baseColor** | shadcn-CLI bot Preset-Auswahl (Nova/Vega/Maia/…); Nova ist die Default-Option mit Lucide+Geist und passt zur User-Entscheidung. `-b radix` für klassische Radix-Primitives. baseColor=neutral als Slate-Basis ohne sichtbaren Akzent. |
| ADR-18 | 2026-04-26  | **Akzentfarbe als Slot, nicht als Wert** | `--accent-brand` in `tokens.css` zeigt initial auf foreground (kein sichtbarer Effekt). Branding-Farbe wird in M3 nach Inhaltsarbeit definiert — eine Zeile Änderung, keine Migration nötig. |
| ADR-19 | 2026-04-26  | **localeDetection bleibt next-intl-Default (`true`)** | User-Entscheidung. Erstbesucher mit Accept-Language=en-* landen auf `/en`. SEO-konform, hreflang-friendly, kein Eingriff in Routing. |
| ADR-20 | 2026-04-26  | **`@gsap/react` `useGSAP` statt manueller `useEffect`** | Auto-Cleanup über `scope`, weniger Boilerplate, offiziell empfohlen ab GSAP 3.13. Alle Animation-Primitives (`FadeInOnScroll`, `RevealText`, `ParallaxImage`) folgen demselben Pattern. |
| ADR-21 | 2026-04-26  | **Reveal-Text via eigene Wort-Split-Logik** | Kein SplitText-Plugin (Club-only / lizenzpflichtig). Whitespace-Split + zwei Span-Schichten (Mask-Container + Inner-Span) reicht für Wort-Stagger; minimaler Code. |
| ADR-22 | 2026-04-26  | **Akzentfarbe = Datenraum-Blau `oklch(0.55 0.16 250)`** | User-Entscheidung in M3. Kühl, technisch, vertrauenserweckend, passt zur „Datenraum"-Konnotation und unterscheidet die Site optisch von typischen Theater-Rot-Tönen. Ergänzt um `--accent-brand-foreground: oklch(0.985 0 0)` für Text auf Akzent-Flächen. |
| ADR-23 | 2026-04-26  | **Page-Content in `src/content/{locale}/*.json` statt `messages/*.json`** | `messages/` für UI-Strings (Buttons, Labels, Page-Titel), `content/` für längere Page-Bodies. Loader (`src/lib/content/loader.ts`) importiert alle Bundles statisch (kein dynamic `import()` — Turbopack-friendly), liefert Locale-spezifische Variante mit DE-Fallback. |
| ADR-24 | 2026-04-26  | **Coming-Soon-Stubs für Blog/FAQ/Termine** | Routen sind in `routing.ts` und im Header-Nav, aber inhaltlich M4. Statt 404 oder Nav-Änderung: minimaler Server-Component-Stub via `<ComingSoonHero>` mit i18n-Strings aus `comingSoon.*`. M4 ersetzt sie mit Supabase-getriebenen Pages. |
| ADR-25 | 2026-04-26  | **Sichtbare TODO-Platzhalter für Impressum/Datenschutz** | User-Entscheidung. Kein generischer DSGVO-Boilerplate (rechtlich nicht final), kein Eigen-Text (Auftraggeber liefert). `legal.json` markiert mit `todo: true`, `<TextSection eyebrow="TODO">` macht den Status sofort sichtbar. |
| ADR-26 | 2026-04-27  | **i18n-Schema via Translation-Tabellen** (`*_translations` 1:N) statt JSON-Spalte oder per-Locale-Tabellen | Sauberer FK + RLS pro Locale, gut indexierbar, einfacher Content-Vergleich, identisch zum Pattern in MUSTER.md. JSON-Spalte hätte Lokalisierungs-Lücken versteckt; per-Locale-Tabellen hätten den Schema-Pflegeaufwand verdoppelt. |
| ADR-27 | 2026-04-27  | **Graceful Degradation auf `ComingSoonHero`** wenn Supabase-Env fehlt | `isSupabaseConfigured()` prüft Public-Keys zur Laufzeit. Pages haben dreistufige Logik: keine Env → ComingSoonHero (alter M3-Look); Env + leere DB → ComingSoonHero mit `empty.*`-Texten; Env + Daten → echte Liste. So bricht `pnpm build` nie und der User kann Vercel deployen, bevor das Cloud-Projekt steht. |
| ADR-28 | 2026-04-27  | **Hand-rolled `src/types/database.ts` initial**, später ersetzt durch `supabase gen types --linked` | Wir können nicht generieren, solange `supabase link` keine Cloud-DB hat. Hand-rolled Datei folgt dem Auto-Gen-Format, sodass `pnpm gen:types` sie sauber überschreibt. Ohne diese Datei wären die Helper alle `any`. |
| ADR-29 | 2026-04-27  | **`revalidatePath('/[locale]/<route>', 'page')`** statt `revalidateTag` im Webhook | Tag-basierte Invalidation würde `cacheComponents: true` + `'use cache'` an jedem Query-Helper voraussetzen. `revalidatePath` arbeitet mit den Route-Files (Pattern matched beide Locales in einem Aufruf) und benötigt nur `export const revalidate = 60` an den Pages. Tag-Pattern kann in M8 nachgezogen werden, falls feinere Kontrolle nötig wird. |
| ADR-30 | 2026-04-27  | **Locale als Postgres-Enum** (`locale` mit `'de' | 'en'`) | Eine Quelle der Wahrheit zwischen DB und next-intl-Routing. Constraint im Schema, kein Spelling-Drift, einfacher Filter `eq('locale', locale)`. Erweiterung um weitere Locale später via `ALTER TYPE locale ADD VALUE 'fr'`. |
| ADR-31 | 2026-04-30  | **Public-Read-Queries über `getSupabaseAnon()`**, nicht den Cookie-bewussten Server-Client | `getSupabaseServer()` ruft `cookies()` und triggert damit in Next.js 16 einen Dynamic-Switch — Pages werden zu ƒ statt ●. Public-Read-Queries (Posts, Events, FAQs) brauchen keinen User-State, also nutzen sie einen reinen Anon-Client (`@supabase/supabase-js` `createClient`, ohne Session-Persistenz). Pages bleiben SSG/ISR-fähig, `generateStaticParams` läuft sauber. Cookie-Server-Client bleibt verfügbar für Forms/Auth ab späteren Milestones. |
| ADR-32 | 2026-04-30  | **Revalidate per `pg_net`-Trigger direkt in Supabase Cloud** statt Studio-Hook | Die Supabase-Studio-Webhook-Funktion ist intern ebenfalls `pg_net`. Weil die Production-URL jetzt feststeht und der Hook testbar ist, wurde `pg_net` aktiviert und eine gemeinsame Trigger-Funktion `public.revalidate_nextjs_cache()` direkt in der Cloud-DB angelegt. Die Secret-haltige URL wird nicht als Repo-Migration committed; bei Secret-Rotation oder Domainwechsel muss die Cloud-Funktion aktualisiert werden. |
| ADR-33 | 2026-04-30  | **`.vercelignore` schützt CLI-Deployments vor lokalen Env-/Tooling-Dateien** | Der erste CLI-Deploy erkannte lokale `.env`-Dateien im Upload-Kontext. `.vercelignore` schließt `.env*`, `.vercel/`, `.claude/`, `.next/`, `node_modules/`, Obsidian-Config und `supabase/.temp/` aus. Finaler Redeploy lief ohne `.env`-Warnung. |
| ADR-34 | 2026-05-01  | **Wikimedia-Locator-Karte als statisches SVG + lineares Equirectangular-Mapping** für Partner-Karte | `Germany_location_map.svg` (NordNordWest, public domain, 463 KB) liegt unter `public/maps/germany.svg`. Bounding-Box `N=55.1°, S=47.2°, W=5.5°, E=15.5°`. Hotspots werden via simpler Linear-Interpolation `x=(lng-W)/(E-W)`, `y=(N-lat)/(N-S)` als Prozent positioniert — der "150% N/S-Stretch" der Karte ist im Asset bereits eingebacken, das lineare Mapping trifft die Position korrekt. Kein Leaflet, kein D3-Geo, kein TopoJSON; passt zur bestehenden Architektur (siehe ADR-11). Hotspots sind ein absolut positioniertes `<button>`-Layer über dem `<img>`, GSAP animiert nur den Pulse-Ring. |
| ADR-35 | 2026-05-06  | **SEO-Layer M8: Layout-Default + per-Page `pageMetadata`-Helper, Sitemap auf App-Ebene, lokalisiertes OG-Image, Hex-Akzentfarbe in `ImageResponse`** | Layout setzt `metadataBase` + Default-OG/Twitter/Robots/`title.template`; jede Page baut ihre eigene Metadata über den `pageMetadata({ locale, href, title?, description? })`-Helper aus `src/lib/seo/alternates.ts`, der `buildAlternates(locale, href)` über `next-intl`-`getPathname` wraped und `canonical` + `languages.{de,en,x-default}` korrekt mit Slug-Übersetzungen aus `routing.ts` füllt. Twitter-Card wird im Helper hartkodiert auf `summary_large_image`, weil ein object-level Override im Page sonst die Layout-Default verschlucken würde. Sitemap (`src/app/sitemap.ts`) liegt **außerhalb** `[locale]/`, weil Sitemaps site-weit sind; sie listet STATIC_HREFS × beide Locales mit `xhtml:link rel="alternate"` und ergänzt Blog-Slugs gracefully (über `isSupabaseConfigured()`-Gate, damit der Build ohne Supabase-Env nicht bricht — siehe ADR-31). OG-Image liegt unter `[locale]/opengraph-image.tsx` (eine Variante pro Locale, kein `generateImageMetadata` nötig); `ImageResponse` rendert OKLCH unzuverlässig, daher die Akzentfarbe als Hex `#2660d8` statt `oklch(0.55 0.16 250)` (siehe ADR-22). Top-Level Convention Files (`/icon`, optional `/apple-icon`, `/manifest`) müssen aus dem next-intl-Proxy-Matcher exkludiert werden, sonst werden sie auf `/<locale>/icon` redirected und brechen mit `InvariantError`. CI ist eine GitHub-Action mit lint/typecheck/build, **ohne Supabase-Env** — der Build prüft strukturell durch (graceful Fallback auf ComingSoonHero), Live-Daten-Validation passiert über Vercel-Preview-Deploys. |
| ADR-36 | 2026-05-06  | **Satori erlaubt in `ImageResponse` nur die CSS-Display-Werte `flex`, `block`, `contents`, `none`, `-webkit-box`** — `inline-block` führt zu HTTP 500 in Production | Satori (das Engine hinter `next/og`'s `ImageResponse`) ist eine eigenständige CSS-Implementation, kein Browser-CSS. `display: inline-block` wirft beim Rendering einen Pipe-Error (`failed to pipe response`, cause: `Invalid value for CSS property "display"`). Lokal mit `pnpm start` bekommt der Caller HTTP 200 mit Content-Type `image/png`, weil der Header geschrieben wird, **bevor** das Satori-Rendering startet — der Stream bricht dann mit Body-Fehler ab. Vercel meldet HTTP 500 mit `x-matched-path: /500`. Lehre: in jedem `ImageResponse`-Tree konsequent `display: "flex"` setzen (auch für leere Span-Akzente, Punkte, Spacer); Pixelgrößen über `width`/`height`. Generell für Satori-JSX: zwei Children im selben Container brauchen einen expliziten `flex`-Parent, sonst meckert Satori; `<br/>` und Inline-Text mit Geschwistern führen ebenfalls zu Problemen. |
| ADR-37 | 2026-05-07  | **View Transitions API über `experimental.viewTransition: true` + Reacts `<ViewTransition>`** statt manuelles `document.startViewTransition` | Next.js 16 hat das Feature offiziell hinter dem Experimental-Flag (Schema in `node_modules/next/dist/server/config-schema.js:316`, Doc unter `node_modules/next/dist/docs/01-app/02-guides/view-transitions.md`). Reacts `<ViewTransition>`-Component aktiviert ihre Animationen nur innerhalb von Transitions/Suspense/`useDeferredValue` — Next.js triggert eine Transition automatisch bei jeder Soft-Navigation, also reicht ein `<Link>` aus `@/lib/i18n/navigation`. Manuelles `document.startViewTransition` würde Reacts Transition-Lifecycle umgehen und mit GSAP-Animationen konkurrieren. Erstes Morph-Pair: PostCard-Cover ↔ PostArticle-Cover via `<ViewTransition name={`post-cover-${slug}`}>`. Sichtbar erst, sobald Posts ein `cover_image_url` haben. TypeScript braucht `/// <reference types="react/canary" />` (in `src/types/react-canary.d.ts`), weil das Symbol nur in `@types/react/canary.d.ts` deklariert ist; Runtime kommt aus Nexts gebundeltem `react`. CSS: globale Reduced-Motion-Regel auf `::view-transition-{old,new,group}(*)` in `globals.css`. |
| ADR-38 | 2026-05-07  | **Hero auf der Landing bleibt textbasiert** — keine Parallax-Animation auf den Text-Block, stattdessen konsolidierte Stagger-Konstante (`STAGGER=0.08`) + statischer dekorativer Akzent-Blob (`bg-[oklch(0.55_0.16_250/_0.10)] blur-3xl`) | Plan-Agent verglich drei Optionen: (a) `translateY`-Scrub auf den gesamten Hero-Text-Block, (b) parallaxer Akzent-Blob, (c) nur Reveal-Choreografie konsolidieren + statisches Element. (a) liest sich auf Apple-Niveau billig und kollidiert mit der Wort-Stagger-Animation in `RevealText`. (b) zieht in einer text-only-Hero die Aufmerksamkeit auf das Hintergrund-Element. (c) ist Apple-like minimal: Stagger-Delays kaskadieren Kicker→Title→Subtitle→CTA mit 80 ms-Schritten, der Blob ist subtil, statisch, decorativ. Sobald das echte Hero-Visual vom User geliefert ist, wird der Blob durch `<ParallaxImage>` ersetzt; die Stagger-Konstante bleibt. |
| ADR-39 | 2026-05-07  | **Layout-globaler `<ScrollTriggerRefresher />`** als Single-Listener auf `usePathname()`-Changes, statt `revertOnUpdate` pro `useGSAP` oder Per-Component-Listener | Soft-Navigation in App-Router triggert kein `load`-Event, also müssen ScrollTrigger-Positions nach jedem Routenwechsel neu berechnet werden, wenn sich Layout-Höhe oder Section-Positionen ändern. `revertOnUpdate` aus `@gsap/react` 2.1.2 reagiert nur auf Dependency-Changes innerhalb derselben Component-Instanz, nicht auf Route-Wechsel — falsches Tool. Der Refresher hängt einmal in `[locale]/layout.tsx` neben `NextIntlClientProvider`, ist Render-`null` und ruft nach `requestAnimationFrame` ein `ScrollTrigger.refresh()`, sodass der DOM-Layout-Pass des neuen Routes durch ist. Greift in keine bestehende `useGSAP`-Komponente ein, respektiert die existierende `registerScrollTrigger`-Lazy-Logik. |
| ADR-40 | 2026-05-07  | **Sitemap-`lastModified` kommt aus stabilen Inhaltsdaten** — statische Seiten nutzen ein festes Content-Datum, Blog-URLs nutzen `posts.published_at`; Post-Revalidate invalidiert auch `/sitemap.xml` | Ein pauschales `new Date()` in `src/app/sitemap.ts` macht jede Sitemap-Generierung zu einem künstlichen Änderungsereignis und sagt Suchmaschinen nicht, wann ein Blogpost tatsächlich veröffentlicht wurde. Next.js 16 erlaubt `lastModified?: string | Date` in `MetadataRoute.Sitemap`; daher liefert `listPublishedPostSitemapEntries()` nur `slug` + `published_at` über den bestehenden `getSupabaseAnon()`-Pfad, ohne Cookie-/Dynamic-Switch. Statische Marketing-Seiten bekommen `STATIC_CONTENT_LAST_MODIFIED`, weil es dafür keine DB-Quelle gibt. Da Metadata-Routes gecacht werden, ergänzt der Revalidate-Endpoint bei `posts` und `post_translations` den literal Route-Handler-Pfad `/sitemap.xml`; dynamische Page-Patterns behalten ihren `type: "page"`. Blog-Detail-URLs werden weiterhin über `getPathname()` gebaut, damit next-intl die Routing-Wahrheit bleibt. |
| ADR-41 | 2026-05-07  | **Original-Site-Bilder aus Sanity-CDN remote rendern** statt neue Binärdateien ins Repo zu kopieren | Die DACH-Netzwerkkarte und vier Portraits liegen bereits öffentlich im Sanity-CDN der alten Website. Wegen der Repo-Regel, keine unnötigen Binaries zu stagen, werden diese Assets über `next/image` remote gerendert und `next.config.ts` erlaubt exakt `cdn.sanity.io/images/lc7slax2/production/**`. Vorteil: kein neues Binary-Churn, schnelle Umsetzung, alte Originalqualität bleibt erhalten. Risiko: Abhängigkeit vom alten CDN; falls dieses später abgeschaltet wird, können die URLs lokal nach `public/maps/` bzw. `public/team/` gespiegelt werden, ohne die Content-Struktur zu ändern. |
| ADR-42 | 2026-05-08  | **CSS-basierte Blog-Cover-Fallbacks** bleiben im Code, auch wenn später echte `cover_image_url`-Assets kommen | Die Live-Posts haben weiterhin keine echten Cover-Bilder. Ohne Fallback wirken Blog-Liste und Detailseiten textlastig, und das ViewTransition-Morph-Pair existiert nur konditional. `PostCoverVisual` rendert deshalb ein rein CSS-basiertes visuelles Cover mit Datum, ORIF-Marker, Grid/Glow und demselben `ViewTransition`-Namen pro Slug. Sobald Supabase `cover_image_url` gesetzt ist, gewinnt automatisch das echte Bild via `next/image`; der Fallback bleibt als robuste Default-Ansicht für neue Posts ohne Asset. Keine neuen Binaries, keine erfundenen Fotos, kein zusätzlicher CMS-Workflow. |
| ADR-43 | 2026-05-11  | **`website-design-ultra` als repo-lokales Codex-Plugin** statt Home-only-Plugin | Der User lieferte ein Design-Plugin-ZIP und fragte nach einem Codex-Plugin „daraus". Repo-lokal ist für dieses Projekt nachvollziehbar, versioniert und mit dem Projekt-Vault dokumentiert. `.agents/plugins/marketplace.json` verweist auf `./plugins/website-design-ultra`, das Manifest liegt unter `.codex-plugin/plugin.json`, die Skills bleiben unter `skills/`. Die übernommenen `commands/*.md` werden als Workflow-Vorlagen behalten, weil Codex-Plugin-Manifest aktuell primär Skills referenziert. |
| ADR-44 | 2026-05-11  | **Editorial/Magazine-Redesign als neues Website-Leitsystem** statt Apple-/Glow-Refresh | Der User wollte die gesamte Website zeitungshaftiger, kreativer und smooth animiert. Die neue Direktion nutzt `Newsreader` als Display-Serif, Geist als Body, Papier/Tinte/Datenraum-Blau/Rubrik-Rot, Zeitungskopf, Linienraster, Drop-Caps und kleinere Radien. Bestehende Inhalte, Routen und Supabase-Queries bleiben unverändert. Glow-Blobs und große weiche Cards wurden reduziert, weil sie eher SaaS/Apple als Zeitung wirken. Typografie skaliert per Breakpoint statt kontinuierlich mit Viewport-Breite; Motion bleibt über bestehende GSAP-Komponenten und globale Reduced-Motion-Regeln kontrolliert. |
| ADR-45 | 2026-05-11  | **Vercel-Artefakte werden im ESLint-Config global ignoriert** | `eslint.config.mjs` überschreibt die Default-Ignores von `eslint-config-next`. Lokale `vercel build`/`deploy --prebuilt`-Artefakte unter `.vercel/output` enthalten generierte CommonJS-Launcher und minimierte JS-Chunks; diese Dateien sind kein Quellcode und erzeugten tausende Lint-Fehler. `.gitignore` ignorierte `.vercel` bereits, aber ESLint braucht die separate `globalIgnores`-Regel für `.vercel/**`. |
| ADR-46 | 2026-05-19  | **Corporate Identity ersetzt Editorial-Direktion vollständig** | User lieferte `STD-Colors+Fonts.pdf` als verbindliche CI: Public Sans als einzige Schrift, Black-Gray-Purple-Palette, keine zweite Akzentfarbe. Der Editorial-Stand aus ADR-44 (Newsreader-Serif + Datenraum-Blau + Rubrik-Rot) wird komplett zurückgebaut. `tokens.css` hält die CI-Hex-Werte als Single-Source-of-Truth; `--accent-brand` ist die einzige Akzent-Variable. Für Text auf Lucent White wurde der Helper-Wert `--accent-brand-ink #6B4A9A` ergänzt, weil Purple Dark `#BF93E1` auf weißem Grund AA-Kontrast nicht erreicht. |
| ADR-47 | 2026-05-19  | **URL-Renames `/projekt`→`/konzeption` und `/beteiligung`→`/jetzt-mitmachen` mit 308-Redirects** statt reines Label-Rename | User-Entscheidung in der Plan-Phase: echte semantische URLs statt nur Menü-Label. Alte Pfade werden via `next.config.ts.redirects()` permanent (HTTP 308) auf die neuen geleitet. Damit bleiben Inbound-Links und Press-Mentions funktional, SEO-Wert wandert über, aber die sichtbaren URLs sind ab sofort CI-konsistent. Pflichtaufgabe pro neuer Route: Eintrag in `routing.ts.pathnames`, `sitemap.ts.STATIC_HREFS`, `pageMetadata({ href: ... })`. |
| ADR-48 | 2026-05-19  | **Header-Menü auf 4 Items reduziert** — Konzeption, Jetzt mitmachen, Materialien, FAQ | Aus `Anmerkungen Entwurf 14.5.2026.md`: Blog/Termine raus, weil das Projekt im Pausenmodus ist und keine neuen Beiträge kommen; Ansprechpersonen raus, weil die genannten Personen nicht mehr aktiv ansprechbar sind. Die entfernten Routen bleiben erreichbar über den Footer und direkte URLs. Die Editorial-Top-Rail im Header („AUSGABE 03 · Datenraum Kultur · JSON / ORIF / DRK") entfällt komplett — die CI hat keinen Zeitungskopf. Stattdessen ist links jetzt das Bühnenverein-Logo prominent als Trust-Signal. |
| ADR-49 | 2026-05-19  | **YouTube via `youtube-nocookie.com`-Iframe statt React-Player-Library** | Für das Image-Video `B4vQwW5ICk8` reicht ein nativer `<iframe loading="lazy" src="https://www.youtube-nocookie.com/embed/...">`. Vorteile: kein zusätzliches NPM-Paket, kein Client-Component-Boilerplate, DSGVO-freundlicher Nocookie-Host (keine Tracking-Cookies bis zum Klick auf Play), und Lighthouse-Performance bleibt nahezu unverändert. `next.config.ts.images.remotePatterns` enthält `i.ytimg.com` für optionale Poster-Bilder. |
| ADR-50 | 2026-05-24  | **Welle 1 (`b7ade71`) per `git merge --ff-only` auf `main` ziehen, statt im Worktree weiterzubauen** | Die als „erledigt" gemeldete Welle 1 lag ungemergt im Worktree-Branch `claude/heuristic-hawking-2024ca`, genau ein Commit vor `main` (M16 `f109bf2`). Ein Fast-Forward ist verlustfrei (keine Merge-Commits, kein Rebase) und bringt den Stand, den der User für live hält, auf `main` — Voraussetzung für die Pflicht-Push-Routine. Die drei übrigen Worktrees (`charming-perlman`, `upbeat-banach`, `wonderful-lamarr`) liegen auf/hinter `main` und wurden ignoriert. |
| ADR-51 | 2026-05-24  | **Google-MyMaps-Iframe (`MapEmbed`) auf `/jetzt-mitmachen` statt Ausbau der eigenen `PartnerMap`** | User-Entscheidung in der Plan-Phase. Das Feedback wollte „die Karte wie Google MyMaps". Statt die SVG-`PartnerMap` (bleibt auf `…/mitwirkung`) um echtes Pan/Zoom zu erweitern, bettet `MapEmbed` die kuratierte MyMaps-Karte (`mid=1WQiyQkmVpOG9CNYfLoEj3qNlGg52xo8`) als lazy iframe ein — echte Interaktivität sofort, kein Geo-Stack. Off-Brand-Styling der Karte wird durch den CI-Rahmen abgefedert. |
| ADR-52 | 2026-05-24  | **Zeitstrahl als Abschnitt `#zeitstrahl` in `/konzeption` (native `Timeline` + Genially-Embed), nicht als eigene Route; `/blog`+`/termine` 308 auf Konzeption** | Feedback: Blog und Termine sollen „zusammengefasst" als Zeitstrahl zur Konzeption. User wählte „beides kombinieren": native, im CI gebaute vertikale `Timeline` aus den Supabase-`events` + darunter das fertige interaktive Genially (`view.genially.com/67531f7a9e5b6c42deba21f3`) als Vertiefung. Ein Abschnitt statt eigener Route hält die Konzeption als eine Erzählung zusammen und vermeidet eine vierte tote Route im Pausenmodus. Alt-Routen werden permanent umgeleitet; `sitemap.ts` führt sie nicht mehr (Blog-Detailseiten bleiben via `postEntries`). |
| ADR-53 | 2026-05-24  | **Team als Abschnitt in Konzeption + erreichbarer `/team`-Deep-Link; Portrait→Bühne-Cross-Fade als JS-Hover mit optionalem `stage`-Feld** | Feedback: „Ansprechpersonen" raus als Begriff (Personen nicht mehr ansprechbar), Telefon/E-Mail weg, Team zurück in die Konzeption, Bühnen-Hover zurück. Umsetzung: `TeamGrid` als Abschnitt „Hinter dem Projekt" in `/konzeption`, Route `ansprechpersonen`→`team` bleibt als Deep-Link (Footer) mit 308-Redirect. `ContactCard` cross-fadet bei Hover/Focus vom Portrait zu einem optionalen `stage`-Bild (`TeamMember.stage`), `motion-reduce`-safe. Reines CSS-Curtain war nicht möglich, da keine Bühnenfotos auffindbar — `stage` bleibt vorerst leer (Zoom-Fallback), Mechanik ist bereit. |
| ADR-54 | 2026-05-24  | **FAQ-Gruppierung im Server-Component, lokalisierte Kategorie-Labels in `messages`** | Die Supabase-Spalte `faqs.category` enthält Slugs (`grundwissen`, `technik-sicherheit`, `umsetzung-kosten`, `datenraum-kultur`). Die `faq/page.tsx` gruppiert die nach `position` sortierten FAQs nach erstem Kategorie-Auftreten und mappt die vier Slugs über `pages.faq.categories.*` auf lokalisierte Labels (DE/EN). `FaqAccordion` rendert pro Gruppe Zwischenüberschrift + Quick-Jump-Nav (`#faq-<slug>`). Gruppierung im Server-Component (nicht in der Client-Komponente), damit die Labels typsicher aus `getTranslations` kommen und keine dynamischen Message-Keys nötig sind. |

---

## Zukunftige offene ADRs

- **ADR-?? Newsletter** — überhaupt? M3+.
- **ADR-?? Analytics** — Vercel Analytics, Plausible, oder gar nichts? M8.
- **ADR-?? Per-Post-OG-Images für Blog-Detail** — pro Post ein eigenes Bild mit Titel via `generateImageMetadata` + zweite `opengraph-image.tsx` unter `[slug]/`. Aktuell zeigt jeder Blog-Detail das Locale-Default-OG. Sinnvoll, sobald Blog-Posts regelmäßig auf Social geteilt werden.
